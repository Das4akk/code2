import { dirname } from "path";
import { fileURLToPath } from "url";

export default [
    {
        languageOptions: {
            ecmaVersion: "latest",
            sourceType: "module"
        },
        rules: {
            "no-unused-vars": "off"
        }
    }
];
